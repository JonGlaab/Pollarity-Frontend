module.exports = {
    plugins: {
        // Tailwind CSS processes the directives
        tailwindcss: {},
        // Autoprefixer adds vendor prefixes (e.g., -webkit-) for broad browser support
        autoprefixer: {},
    },
}